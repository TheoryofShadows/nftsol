# ============================================
# NFTSol - Secure Git Ignore File
# ============================================

# === CRITICAL: NEVER COMMIT SECRETS! ===
.env
.env.*
*.env
!.env.example
*.key
*.pem
secrets/
wallet.json
**/wallet.json
platform-keys-backup.json
*.secret
*secret*

# Keypair files (Solana wallets)
*keypair.json
temp-keypair.json
**/treasury-keypair.json
**/temp-keypair.json

# API Keys & Credentials
*API_KEY*
*api-key*
*_SECRET*
*_KEY*
PINATA_JWT
HELIUS_API_KEY
PLATFORM_SECRET_KEY*

# Database URLs with passwords
DATABASE_URL=*

# === Node & Dependencies ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.pnpm-store/

# === Build Artifacts ===
dist/
build/
out/
*.tsbuildinfo
*.js.map
*.d.ts.map

# === Vite / Build Caches ===
**/.vite/
.cache/
coverage/

# === Testing & Coverage ===
.nyc_output/
*.lcov
coverage/

# === Database ===
*.sqlite
*.sqlite3
*.db
*.db-journal
backups/

# === Solana ===
.anchor/
target/
test-ledger/
.solana/
*.so

# === Logs ===
logs/
*.log
*.log.*

# === Runtime Artifacts ===
uploads/
temp/
tmp/
*.tmp
*.bak
*.bak.*
*.backup

# === OS ===
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# === IDE & Local Tool Config ===
.claude/
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# === Package Managers ===
# package-lock.json - Keep for deployment (npm ci requires it)
# package-lock.json
yarn.lock
pnpm-lock.yaml

# === Client/Server specific ===
client/.env*
server/.env*
apps/backend/.env*
apps/frontend/.env*

# === Deployment packages ===
*.zip
netlify-deploy-*.zip
nftsol-frontend-deploy.zip

# === Old/Redundant Documentation ===
# Keep only: README.md, WHITEPAPER.md, TECHNICAL-DOCS.md, DEPLOYMENT.md
# All other .md files can be removed or archived
# Archived/unused directories
.archive/

# Local Netlify folder
.netlify

# Legacy server directory (migrated to apps/backend/)
server/

# Auto-generated lefthook boilerplate (lefthook is configured per-package; the
# root file the tool regenerates is empty example config, not real config)
/lefthook.yml
