feat: Complete security fixes, environment separation, and API configuration v2.0.4

## Security
- Fix all fixable vulnerabilities (fast-redact, esbuild)
- Document unfixable bigint-buffer vulnerability (acceptable risk)
- Remove hardcoded secrets from render.yaml
- Create .env.example files for both frontend and backend
- Ensure proper .gitignore configuration

## Environment Separation
- Implement automatic dev/production environment detection
- Development: uses localhost, devnet, safe defaults
- Production: uses production URLs, mainnet, strict enforcement
- Centralized API configuration with environment-aware URLs

## API Configuration
- Create centralized API config (client/src/config/api.ts)
- Update all components to use centralized endpoints
- Fix dynamic import errors with error boundaries
- Ensure production uses https://nftsol.onrender.com
- Ensure development uses http://localhost:3001

## Code Quality
- Fix all TypeScript compilation errors
- Fix linting warnings (unused variables)
- Clean up unused imports
- Improve error handling

## Features
- All Echo components included in build
- Modern design system included
- All functionality working
- Comprehensive error handling

## Build Status
- Backend: Compiles successfully
- Frontend: Builds successfully (8.93s)
- All components included
- Version: v2.0.4

